/**
 * Punishment presets, their ladders, and the records they produce.
 *
 * <p>Staff apply a preset and X-Warden takes the next rung of its ladder, either by dispatching a
 * configured command or by enforcing the step itself. X-Warden never picks a preset on its own
 * unless the server owner configures a check to.
 *
 * @since 1.1.0
 */
package dev.drawethree.xwarden.api.staff.punish;
